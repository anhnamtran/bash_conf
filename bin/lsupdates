#!/usr/bin/bash
#!/bin/bash
CHK=$(checkupdates)
CHKAUR="$(yay -Qu --color=always --aur | sed 's/$/ (AUR)/g')"

if [ ${#CHKAUR} -gt 0 ]; then 
  if [ "$CHK" = "" ]; then
    CHK="${CHKAUR}"
  else
    CHK="${CHK}"$'\n'"${CHKAUR}"
  fi
fi

CHK=$(echo "${CHK}" | sort | uniq)
echo "${CHK}"
