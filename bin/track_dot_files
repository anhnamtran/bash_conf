#!/bin/bash

git init --bare $HOME/.cfg

alias dot_files='git --git-dir=$HOME/.cfg/ --work-tree=$HOME'
dot_files config --local status.showUntrackedFiles no
echo "Alias for dot files is \"dot_files\""
